<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/item_image"
        android:layout_width="@dimen/item_image_size"
        android:layout_height="@dimen/item_image_size"
        android:layout_gravity="center_vertical|end"
        android:layout_margin="@dimen/item_image_margin"
        android:scaleType="centerCrop"
        app:srcCompat="@drawable/roquefort"
        android:contentDescription="@string/item_image_description" />

    <TextView
        android:id="@+id/item_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_weight="1"
        android:paddingBottom="@dimen/item_name_padding_bottom"
        android:paddingStart="@dimen/item_name_padding_start"
        android:textSize="@dimen/item_name_text_size"
        android:text="@string/name_description" />

    <Button
        android:id="@+id/button"
        style="?attr/borderlessButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/item_button_description"
        android:layout_gravity="center_vertical|bottom"
        android:layout_marginEnd="@dimen/item_button_margin_end"
        android:minWidth="@dimen/item_buttom_min_widht"
        android:padding="@dimen/item_button_padding"
        android:paddingStart="@dimen/item_button_padding_start"
        android:paddingEnd="@dimen/item_button_padding_end"
        android:textSize="@dimen/item_button_text_size"
        android:textColor="@color/colorAccent"/>
</LinearLayout>